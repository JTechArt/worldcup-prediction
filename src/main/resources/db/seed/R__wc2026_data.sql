-- =============================================================
-- R__wc2026_data.sql  (Flyway Repeatable Migration)
-- Data is loaded via the bootstrap runner, not seeded here.
-- Run: SPRING_PROFILES_ACTIVE=sqlite,bootstrap FOOTBALL_API_ENABLED=true
--      FOOTBALL_API_KEY=<key> ./mvnw spring-boot:run
-- After bootstrap completes, export DB and replace this file with
-- the correct INSERT statements for repeatable deployment.
-- =============================================================
